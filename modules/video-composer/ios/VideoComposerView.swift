import ExpoModulesCore

/// Placeholder view – Video creation uses createVideo() async function.
/// This view is kept for module compatibility.
class VideoComposerView: ExpoView {
  required init(appContext: AppContext? = nil) {
    super.init(appContext: appContext)
  }
}
